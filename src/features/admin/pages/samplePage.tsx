import { Grid } from '@mui/material'
// import { useQuery } from '@tanstack/react-query'
// import { healthRetrieveOptions } from '../../../sdk/@tanstack/react-query.gen';

export const SamplePage = () => {
  // const { data } = useQuery(healthRetrieveOptions());

  // console.log("data", data);
  
  return (
    <Grid container>
      samplePage
     
    </Grid>
  )
}

